<div class="pink-bg">
  <div class="container">
    <div class="row h-100 justify-content-center align-items-center">
      <div class="col-xs-12 col-sm-10 col-sm-offset-10 col-md-8 col-md-offset-8 col-lg-6">
        <div class="form-styling">
          <h2>Sign up</h2>

          <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
            <%= f.error_notification %>

            <div class="form-inputs">
              <%= f.input :email,
                          required: true,
                          autofocus: true,
                          placeholder: "indigenius@gmail.com",
                          input_html: { autocomplete: "email" }%>
              <%= f.input :first_name,
                          required: true,
                          placeholder: "iNDiGENiUs",
                          input_html: { autocomplete: "first_name" }%>
              <%= f.input :last_name,
                          required: true,
                          placeholder: "iNDiGENiUs",
                          input_html: { autocomplete: "last_name" }%>
              <%= f.input :password,
                          required: true,
                          placeholder: "******",
                          hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                          input_html: { autocomplete: "new-password" } %>
              <%= f.input :password_confirmation,
                          placeholder: "******",
                          required: true,
                          input_html: { autocomplete: "new-password" } %>
              <%= f.input :about,
                          placeholder: "Tell the world a little about yourself",
                          required: false,
                          input_html: { autocomplete: "about" } %>
              <%= f.input :avatar, as: :file %>
            </div>

            <div class="form-actions">
              <%= f.button :submit, "Sign up", class: "button-square-green" %>
            </div>
          <% end %>

          <%= render "devise/shared/links" %>
        </div>
      </div>
    </div>
  </div>
</div>
