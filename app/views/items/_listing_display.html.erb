<% if items.empty? %>

    <table>
        <thead>
            <th colspan="5">No items yet</th>
        </thead>
    </table>

<% else %>

    <table>
        <thead>
        <tr>
            <th id="name">Name</th>
            <th colspan="3">Actions</th>
        </tr>
        </thead>

        <tbody>
        <% items.each do |item| %>
            <tr>
            <td class ><%= item.name %></td>
            <td><%= link_to 'Show', item %></td>
            <td><%= link_to 'Edit', edit_item_path(item) %></td>
            <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
        <% end %>
        </tbody>
    </table>
<% end %>
