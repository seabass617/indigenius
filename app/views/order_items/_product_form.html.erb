<%= simple_form_for [@item, product] do |f| %>
  <%= f.input :quantity,
               collection: (1..@item.quantity),
               label: "Choose quantity"%>
  <%= f.button :submit, 'Add to cart', class: 'indi-btn' %>
<% end %>
